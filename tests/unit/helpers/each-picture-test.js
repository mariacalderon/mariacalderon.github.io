
import { eachPicture } from 'mariacalderon/helpers/each-picture';
import { module, test } from 'qunit';

module('Unit | Helper | each picture');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eachPicture([42]);
  assert.ok(result);
});

