
import { projectPropertyType } from 'mariacalderon/helpers/project-property-type';
import { module, test } from 'qunit';

module('Unit | Helper | project property type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = projectPropertyType([42]);
  assert.ok(result);
});

