
import { projectDescriptionHtmlFormat } from 'mariacalderon/helpers/project-description-html-format';
import { module, test } from 'qunit';

module('Unit | Helper | project description html format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = projectDescriptionHtmlFormat([42]);
  assert.ok(result);
});

